legacy-peer-deps=true
# Force the public npm registry so deploys (Vercel) and local installs match,
# overriding any user-level ~/.npmrc that points to an internal mirror.
registry=https://registry.npmjs.org/
